version: '3.3'

# datavaultbuilder version is defined as DVB_TAG in the file '.env'

services:

  exasol:
    image: 'exasol/docker-db:6.2.10-d1'
    volumes:
            - mydata:/exa
    ports:
      - '8888:8888'
      - '9043:443'
      - '9583:6583'
    privileged: true

volumes:
  mydata:
